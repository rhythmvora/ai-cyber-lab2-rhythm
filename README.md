# ai-cyber-lab2-rhythm

## Setup

Install the Python dependencies required by the project and your script imports (`pandas`, `numpy`, `matplotlib`, `seaborn`, etc.):

```bash
python -m pip install -r requirements.txt
```

`sys` and `warnings` are part of Python's standard library, so they do not need to be installed separately.
